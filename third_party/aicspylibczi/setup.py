from setuptools import setup, find_packages

setup(
    name="aicspylibczi",
    version="3.2.1",  # Adjust version as necessary
    packages=find_packages(),  # Finds all the packages in the directory
)
